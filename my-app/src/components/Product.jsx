function Product ({name, price}) {
    return (
        <div style={{display: 'flex', flexDirection: 'column'}}>
            <div>{name}</div>
            <div>{price}</div>
        </div>
    )}

    export default Product
